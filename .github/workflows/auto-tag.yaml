name: Tag on push

on: 
  push:
    branches:
      main

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          debug: true
          script: |
            rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "refs/tags/1.0.0",
              sha: context.sha
            })
            # var result = await github.rest.git.createTag({
            #   owner: context.repo.owner,
            #   repo: context.repo.repo,
            #   tag: "1.0.0",
            #   message: "",
            #   object: context.sha,
            #   type: "commit"
            # })

            # console.log(result)